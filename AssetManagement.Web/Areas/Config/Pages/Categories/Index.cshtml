@page
@using AssetManagement.Web.Areas.Config.Pages.Categories
@model IndexModel

<table class="table table-sm table-hover table-striped">
    <thead>
        <tr>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model.List!)
        {
            <tr>
                <td><a asp-page="Details" asp-route-id="@item.Id">@item.Name</a></td>
               
            </tr>
        }
    </tbody>
</table>
